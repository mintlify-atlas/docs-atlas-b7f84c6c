---
title: 'Get Client by ID'
description: 'Retrieve a specific client by ID'
api: 'GET /api/v1/clientes/{id}'
---

## Endpoint

```
GET /api/v1/clientes/{id}
```

Retrieve detailed information about a specific client using their unique identifier.

## Path Parameters

<ParamField path="id" type="number" required>
  The unique identifier of the client
</ParamField>

## Response

Returns a client object:

<ResponseField name="id" type="number">
  Unique client identifier
</ResponseField>

<ResponseField name="dni" type="string">
  National ID number (DNI) - 8 digits
</ResponseField>

<ResponseField name="nombreCompleto" type="string">
  Client's full name
</ResponseField>

<ResponseField name="correo" type="string">
  Client's email address (used for notifications and tracking)
</ResponseField>

<ResponseField name="telefono" type="string">
  Client's phone number - 9 digits
</ResponseField>

## Example Request

```bash
curl -X GET http://localhost:8080/api/v1/clientes/1
```

## Example Response

```json
{
  "id": 1,
  "dni": "12345678",
  "nombreCompleto": "María García",
  "correo": "maria.garcia@email.com",
  "telefono": "987654321"
}
```

## Error Responses

### Client Not Found

```json
{
  "error": "ClienteNotFoundException",
  "message": "Cliente no encontrado con id: 999"
}
```

<Warning>
This endpoint throws `ClienteNotFoundException` if the client with the specified ID does not exist.
</Warning>

## Use Cases

- Display client details in admin dashboard
- Verify client information before creating shipments
- Client profile management
- Integration with customer service systems

## Related Endpoints

- [List All Clients](/api/clients/list) - Get all clients
- [Search Clients](/api/clients/search) - Search by keyword
- [Get Client Shipments](/api/clients/overview) - Get paginated shipment history (GET /api/v1/clientes/{clienteId}/envios)
- [Create Client](/api/clients/create) - Register new client
